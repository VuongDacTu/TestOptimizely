@inherits Blend.Cms12.Views.AlloyPageBase<PageViewModel<MallPage>>
@{
    Layout = "~/Views/Shared/Layouts/_Root.cshtml";
}

@{
    await Html.RenderPartialAsync("Breadcrumbs", Model);
}

<div class="ll-mall ll-frame frame-size-80 padding-box-top-bot-5 border-bottom-dark">
    @{
        foreach (var item in Model.CurrentPage.MainContentArea?.FilteredItems ?? Enumerable.Empty<ContentAreaItem>())
        {
            var content = item.GetContent();

            if (content != null)
            {
                Html.RenderContentData(content, false);
            }
        }
    }
</div>